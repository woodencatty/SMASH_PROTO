extends layout

block content
  h1  정리운동(등근육펴기)
  p#ViewTimer

  input#btnMain(name='btnMain', type='button', value='I wanna stop.', onclick="location.href='/main'")

  img(src='/images/move3.gif' width='506', height='1400' alt="muk-pa")

  script(type='text/javascript').
    var SetTime = 10;
    function msg_time() {
    m = Math.floor(SetTime % 60);
    var msg =  m ;
    document.all.ViewTimer.innerHTML = msg;
    SetTime--;
    if (SetTime < 0) { clearInterval(tid); location.replace('/done'); }
    }
    window.onload = function TimerStart(){ tid=setInterval('msg_time()',1000) };
